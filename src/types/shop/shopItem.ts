export interface IDataShop {
  image: string[];
  name: string;
  price: string;
  oldPrice: string;
  desc: string;
  url: string;
  id: string;
}

export interface SizeData {
  size: string;
  checked: boolean;
  id: number;
}
